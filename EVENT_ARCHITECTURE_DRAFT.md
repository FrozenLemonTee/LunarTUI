# LunarTUI 事件交互架构草案

## 目标

LunarTUI 维持为一个终端 UI 组件库，只负责描述组件、布局、渲染和事件响应接口，不直接依赖任何具体终端事件库。

终端事件能力由外部事件库提供。LunarTUI 只定义一套终端无关、可插拔的接口，任何符合该接口的事件后端都可以与之组合。

## 总体原则

1. TUI 库只暴露抽象，不绑定实现。
2. 事件后端只负责采集、解析和归一化终端事件。
3. 应用层负责把事件路由到组件树，并决定何时重绘。
4. 默认实现可以存在，但不应成为库的唯一依赖。
5. MoonBit 对外接口保持 MoonBit 风格，底层实现可以通过 C ABI 和 FFI 承接。

## 推荐分层

### 1. LunarTUI 核心层

这一层只定义组件协议和事件协议，不关心事件来自哪里。

建议新增的核心概念包括：

- `Event`：统一的输入事件表示
- `EventResult`：事件处理结果，例如已处理、未处理、需要重绘
- `HitTest` 或 `Focus` 相关能力：用于命中测试和焦点管理
- `Widget` 的事件处理接口：用于组件响应键盘、鼠标、拖拽等事件

核心层的职责是：

- 定义事件模型
- 定义组件如何接收事件
- 定义事件是否被消费
- 定义组件是否请求重绘

它不应该：

- 读取 stdin
- 解析终端原始字节流
- 绑定某个特定平台事件库
- 假定事件一定来自某个固定后端

### 2. 终端事件库层

这一层是默认事件实现，但本质上仍然是可替换后端。

建议结构是：

- 核心逻辑用 C++ 编写
- C++ 核心只暴露 C ABI
- MoonBit 外壳通过 C FFI 调用 C ABI
- MoonBit 外壳向上提供与 LunarTUI 对齐的事件接口

这一层的职责是：

- 打开终端 raw mode
- 监听键盘、鼠标、拖拽、窗口尺寸变化等输入
- 将平台事件翻译成 LunarTUI 的通用事件结构
- 向上游输出事件流或轮询接口

这一层不应该：

- 直接依赖 LunarTUI 的具体组件实现
- 假设上游一定使用某种布局或某种焦点策略
- 暴露 C++ 类型到 MoonBit 外部

### 3. 上游应用层

应用层负责把事件和组件树连接起来。

建议在上游项目中做：

- 初始化事件后端
- 维护当前焦点组件
- 决定事件路由顺序
- 事件处理后触发重新绘制

这样 LunarTUI 仍然保持纯 UI 组件库的定位，而不会变成一个绑定输入后端的应用框架。

## 建议的接口方向

### 事件模型

建议事件模型尽量稳定、后端无关，并覆盖常见输入：

- 键盘按下、释放、重复
- 鼠标移动、按下、抬起、滚轮
- 拖拽开始、拖拽移动、拖拽结束
- 窗口大小变化
- 焦点进入和离开
- 原始文本输入

如果要控制复杂度，可以先从最小子集开始：

- `Key`
- `Mouse`
- `Resize`
- `TextInput`
- `Focus`

### Widget 协议

当前 `Widget` 只有尺寸和渲染能力。要支持交互，建议增加事件处理能力，但不要强迫所有组件都维护复杂状态。

推荐形式是：

- 默认返回未处理
- 需要交互的组件按需覆写
- 容器组件负责把事件继续向子组件分发

如果未来希望支持更精细的交互，还可以再补：

- 命中测试
- 焦点链
- 鼠标捕获
- 局部失效重绘

### 事件结果

建议事件处理函数返回一个明确结果，而不是只返回 `Unit`。

例如：

- 是否处理成功
- 是否请求重绘
- 是否继续向父级传播
- 是否继续向其他兄弟节点传播

这样能把“事件消费语义”和“重绘语义”分开，后面扩展会更稳。

### 跨组件状态联动

仅有事件消费结果还不够覆盖所有交互场景。某些情况下，一个组件的交互需要驱动其他组件状态变化，例如：

- 编辑区修改光标位置后，状态栏同步显示当前位置。
- 某个按钮触发后，列表视图切换选中项。
- 输入内容变化后，预览组件立即刷新。

这类需求不建议让组件之间直接互相持有引用并相互修改，而是通过一层统一的动作或消息机制来完成。

建议把这一层放在事件协议之上，作为一种面向应用层的中转语义：

- Widget 处理事件后，可以选择发出一个或多个语义化动作。
- 应用层或统一 dispatcher 接收这些动作。
- dispatcher 更新共享状态模型。
- 依赖该状态的其他组件重新渲染。

这样可以保留可插拔性，同时实现类似 Qt 信号-槽的跨组件联动效果。

### 建议的语义边界

为了避免组件耦合，建议保持以下边界：

- Widget 只知道自己的局部事件处理逻辑。
- Widget 可以产生动作，但不直接驱动其他 Widget。
- 应用层负责把动作映射到共享状态或回调。
- 共享状态变化后，由顶层统一触发重绘。

如果未来需要更强的表达能力，可以在现有 EventResult 之上继续扩展一个 Action 或 Message 层，但这层不应依赖具体终端事件后端。

## 推荐的组合方式

### 方式一：应用层手动组合

这是最直接、也最符合可插拔原则的方式。

流程如下：

1. 应用初始化 LunarTUI 组件树。
2. 应用初始化某个终端事件库。
3. 应用不断从事件库读取事件。
4. 应用把事件交给当前根组件或焦点组件处理。
5. 事件处理后决定是否重绘。

优点：

- 组合关系最清晰
- 依赖方向最简单
- 最容易替换后端

### 方式二：提供一个薄封装运行器

如果后续需要更方便的默认用法，可以在外部单独提供一个运行器层。

这个运行器可以：

- 拉起事件循环
- 维护焦点
- 自动重绘
- 处理窗口尺寸变化

但运行器不应进入 LunarTUI 核心库，否则会把库边界拉得过宽。

## 闭环验证示例

在完成最小事件闭环后，建议实现一个上游示例项目：终端文本编辑器。

这个 demo 的作用不是追求功能完整，而是验证整条链路是否真的打通：

1. 终端事件库可以稳定采集键盘输入和方向键事件。
2. LunarTUI 可以接收事件并驱动组件状态变化。
3. 组件可以根据焦点或光标状态改变渲染结果。
4. 事件处理后能够触发局部或整体重绘。
5. 用户输入的缓冲区内容可以写回文件。

### Demo 行为目标

这个文本编辑器示例建议至少具备以下行为：

- 光标持续闪烁，用于提示当前编辑位置。
- 输入普通字符后，字符回显到终端缓冲区中。
- 通过方向键移动光标位置。
- 通过 `Ctrl+S` 将当前缓冲区文本写入文件。

### Demo 能验证的关键点

- 输入后端是否真正实现了轮询或事件推送闭环。
- 事件模型是否足够表达键盘输入、光标移动和保存动作。
- Widget 是否具备处理输入并更新内部状态的能力。
- 终端重绘是否能在交互过程中保持稳定。
- 文件写入是否能在上游项目中顺利接入，而不污染 LunarTUI 核心。

### 这个 demo 对接口设计的反向约束

如果要让这个 demo 成立，接口至少需要满足：

- 可以识别按键种类和修饰键组合。
- 可以区分普通输入和控制命令。
- 可以把光标位置作为组件状态管理。
- 可以在状态变化后请求重绘。
- 可以让上游应用在不修改 LunarTUI 核心的前提下接入文件 I/O。

## 对当前方案的评价

你的方案总体上是合理的，而且比直接把输入逻辑塞进 LunarTUI 核心更健康。

它的优点是：

- 保持 LunarTUI 的纯渲染定位
- 默认实现和接口抽象分离
- C++ 核心可以专注于事件采集与解析
- MoonBit 外壳可以对齐 LunarTUI 的类型系统
- 上游项目可以自由替换事件后端

需要注意的风险是：

- 不要把具体后端类型泄漏到 LunarTUI 公共 API
- 不要让事件模型和某个事件库的语义强绑定
- 不要把焦点管理、命中测试、重绘策略全部塞进一个接口里
- 不要让容器组件只会渲染而没有事件分发路径

## 建议的落地顺序

1. 先在 LunarTUI 核心中补最小事件抽象。
2. 再定义组件事件处理和传播语义。
3. 然后编写默认事件后端的 MoonBit 外壳接口。
4. 最后实现 C++ 核心和 C ABI。
5. 通过一个上游示例项目验证组合方式。

## 结论

这是一条合理的升级路线，核心前提是把“事件模型”与“事件后端”彻底分离。

只要 LunarTUI 保持只暴露通用接口，而默认事件库只负责实现这些接口，你的方案就能同时满足：

- 可插拔
- 可替换
- MoonBit 友好
- 对现有 TUI 代码侵入较小
